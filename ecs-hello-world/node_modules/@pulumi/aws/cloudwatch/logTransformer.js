"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
Object.defineProperty(exports, "__esModule", { value: true });
exports.LogTransformer = void 0;
const pulumi = require("@pulumi/pulumi");
const utilities = require("../utilities");
/**
 * Resource for managing an AWS CloudWatch Logs Transformer.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const exampleLogGroup = new aws.cloudwatch.LogGroup("example", {name: "example"});
 * const example = new aws.cloudwatch.LogTransformer("example", {
 *     transformerConfigs: [{
 *         parseJsons: [{}],
 *     }],
 *     logGroupArn: exampleLogGroup.arn,
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import CloudWatch Logs Transformer using the `log_group_arn`. For example:
 *
 * ```sh
 * $ pulumi import aws:cloudwatch/logTransformer:LogTransformer example arn:aws:logs:us-west-2:123456789012:log-group:example
 * ```
 */
class LogTransformer extends pulumi.CustomResource {
    /**
     * Get an existing LogTransformer resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new LogTransformer(name, state, { ...opts, id: id });
    }
    /**
     * Returns true if the given object is an instance of LogTransformer.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === LogTransformer.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["logGroupArn"] = state?.logGroupArn;
            resourceInputs["region"] = state?.region;
            resourceInputs["transformerConfigs"] = state?.transformerConfigs;
        }
        else {
            const args = argsOrState;
            if (args?.logGroupArn === undefined && !opts.urn) {
                throw new Error("Missing required property 'logGroupArn'");
            }
            if (args?.transformerConfigs === undefined && !opts.urn) {
                throw new Error("Missing required property 'transformerConfigs'");
            }
            resourceInputs["logGroupArn"] = args?.logGroupArn;
            resourceInputs["region"] = args?.region;
            resourceInputs["transformerConfigs"] = args?.transformerConfigs;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(LogTransformer.__pulumiType, name, resourceInputs, opts);
    }
}
exports.LogTransformer = LogTransformer;
/** @internal */
LogTransformer.__pulumiType = 'aws:cloudwatch/logTransformer:LogTransformer';
//# sourceMappingURL=logTransformer.js.map