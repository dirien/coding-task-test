"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
Object.defineProperty(exports, "__esModule", { value: true });
exports.TableReplication = void 0;
const pulumi = require("@pulumi/pulumi");
const utilities = require("../utilities");
/**
 * Manages Amazon S3 Tables Table Replication configuration.
 *
 * ## Example Usage
 *
 * ### Basic Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 *
 * const example = new aws.s3tables.TableReplication("example", {
 *     tableArn: exampleAwsS3tablesTable.arn,
 *     role: exampleAwsIamRole.arn,
 *     rule: {
 *         destinations: [{
 *             destinationTableBucketArn: target.arn,
 *         }],
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import S3 Tables Table Replication using the `table_arn`. For example:
 *
 * ```sh
 * $ pulumi import aws:s3tables/tableReplication:TableReplication example 'arn:aws:s3tables:us-west-2:123456789012:table/example-table'
 * ```
 */
class TableReplication extends pulumi.CustomResource {
    /**
     * Get an existing TableReplication resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new TableReplication(name, state, { ...opts, id: id });
    }
    /**
     * Returns true if the given object is an instance of TableReplication.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TableReplication.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["region"] = state?.region;
            resourceInputs["role"] = state?.role;
            resourceInputs["rule"] = state?.rule;
            resourceInputs["tableArn"] = state?.tableArn;
            resourceInputs["versionToken"] = state?.versionToken;
        }
        else {
            const args = argsOrState;
            if (args?.role === undefined && !opts.urn) {
                throw new Error("Missing required property 'role'");
            }
            if (args?.tableArn === undefined && !opts.urn) {
                throw new Error("Missing required property 'tableArn'");
            }
            resourceInputs["region"] = args?.region;
            resourceInputs["role"] = args?.role;
            resourceInputs["rule"] = args?.rule;
            resourceInputs["tableArn"] = args?.tableArn;
            resourceInputs["versionToken"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TableReplication.__pulumiType, name, resourceInputs, opts);
    }
}
exports.TableReplication = TableReplication;
/** @internal */
TableReplication.__pulumiType = 'aws:s3tables/tableReplication:TableReplication';
//# sourceMappingURL=tableReplication.js.map